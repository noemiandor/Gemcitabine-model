# Test fixtures

Panel-F test tables are generated deterministically inside the test temporary directory. They are schema/ordering fixtures only and are not scientific reference data. Canonical state-pathway results must be supplied under the immutable `taoli_state_pathway_etp2_24_day17_v1` data directory.
