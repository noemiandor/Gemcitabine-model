# Test fixtures

Panel-F test tables are generated deterministically inside the test temporary directory. They are schema/ordering fixtures only and are not scientific reference data. Canonical 04i results must be supplied under the immutable `taoli_04i_etp2_24_day17_v1` data directory.
